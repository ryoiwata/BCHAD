Add a 'Payment Methods' management page to the merchant dashboard. Merchants should be able to add, edit, delete, and set a default payment method. Each payment method has a type (credit card, ACH, wire transfer), a human-readable label, and payment credentials stored via our Vault integration (never stored raw). Merchants can mark one payment method as their default. Include a list view with sorting and filtering, a detail view, and a create/edit form. Audit log every change. Gate the feature behind the payment_methods:manage permission scope.
